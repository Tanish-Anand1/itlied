# TODOS (autoplan deferred)

- Hosted sandbox (E2B/Fly) vs DIY Docker — revisit if Windows Docker remains a sink
- Standalone model-proxy process (only if host-module story fails audit)
- Fixture #2 when single-fixture meta dies (dated revisit after 50 matches or &lt;X% resubmit)
- CAPTCHA / stronger abuse controls if open submit is attacked
- Name decision (owners) — design must not invent
